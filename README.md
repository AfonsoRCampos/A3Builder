## Build Project

To be done when first installing the app. Must also be repeated each time there is a new version.

```
npm ci
npm run build
```

## Run App

```
npm run start
```

App should then be available at http://localhost:3000. To stop the app, kill the process on the command line (CTRL + C).